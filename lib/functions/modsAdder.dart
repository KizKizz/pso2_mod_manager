import 'package:cross_file/cross_file.dart';
import 'package:flutter/material.dart';

modFilesAdder(BuildContext context, List<List<String>> sortedModsList, XFile xFile) {
  for (var element in sortedModsList) {
    print(element);
  }
}
